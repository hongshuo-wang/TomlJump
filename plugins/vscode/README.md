# VS Code Extension Placeholder

This folder is reserved for the later VS Code implementation.

The first implementation target is JetBrains.

VS Code work must reuse the shared core classification tests, shared fixtures, and the product decisions already made for JetBrains.

Do not implement the VS Code extension until the JetBrains MVP has passing tests and a buildable artifact.
